// The room, as one import. Mount <Room /> once per page: it renders the dock
// pill, the full-screen player and the breathing module, and owns its own audio
// element, analyser and animation loops.
export { default as Room } from './Room';
export { default } from './Room';
