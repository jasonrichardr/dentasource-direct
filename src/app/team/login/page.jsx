import TeamLoginForm from './TeamLoginForm';

export const metadata = {
  title: 'Team sign in — DentaSource Direct',
  robots: { index: false, follow: false },
};

export default function TeamLoginPage() {
  return <TeamLoginForm />;
}
